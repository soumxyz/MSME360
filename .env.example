# ============================================================
# MSME360 frontend environment
# Copy this file to `.env` and set `VITE_API_URL` to point at your backend.
# `import.meta.env.VITE_*` is inlined by Vite at build time.
# ============================================================

VITE_API_URL=http://localhost:8000/api
